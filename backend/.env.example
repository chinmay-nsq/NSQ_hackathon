DATABASE_URL="postgresql://postgres:postgres@localhost:5432/weatherline"
PORT=4000
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
COOKIE_NAME="weatherline_session"
NODE_ENV="development"
GROQ_API_KEY=""
# Optional extra keys — used as fallbacks when the primary key hits a rate
# limit. Add as many as you have, numbered sequentially with no gaps.
# GROQ_API_KEY_2=""
# GROQ_API_KEY_3=""
CLIENT_ORIGIN_WEB="http://localhost:3000"
